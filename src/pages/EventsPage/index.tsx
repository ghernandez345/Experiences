import TextInput from "components/TextInput";

interface EventsPageProps {}

const EventsPage = ({}: EventsPageProps) => {
  return <TextInput placeholder="Add a link here..." />;
};

export default EventsPage;
